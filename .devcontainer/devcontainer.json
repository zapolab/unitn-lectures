{
  "dockerComposeFile": "docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postStartCommand": "cd /workspace && nohup opencode serve --hostname 0.0.0.0 --port 3000 > /tmp/opencode.log 2>&1 & sleep 3"
}
